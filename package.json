{
  "name": "@resize.to/options-parser",
  "version": "0.0.1",
  "description": "Parse transform options from a URL.",
  "author": "Brandon Aaron <hello.brandon@aaron.sh> (http://brandon.aaron.sh/)",
  "license": "UNLICENSED",
  "main": "index.js",
  "scripts": {
    "lint": "standard --fix",
    "test": "jest --coverage"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "color": "^3.1.0"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "standard": "^12.0.1"
  }
}
