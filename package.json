{
  "name": "@resize.to/options-parser",
  "version": "0.0.3",
  "description": "Parse image transform options from a URL.",
  "author": "Fresh Codes <hello@fresh.codes> (https://fresh.codes/)",
  "repository": "github:resizeto/options-parser",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "lint": "standard --fix",
    "test": "jest --coverage",
    "coveralls": "jest --coverage --coverageReporters=text-lcov | coveralls"
  },
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "color": "^3.1.2"
  },
  "devDependencies": {
    "coveralls": "^3.0.7",
    "jest": "^24.9.0",
    "standard": "^14.3.1"
  },
  "files": [
    "src/errors.js",
    "src/options.js",
    "src/parser.js",
    "index.js",
    "LICENSE"
  ]
}
